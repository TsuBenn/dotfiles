import qs.config
import qs.modules
import qs.services

import QtQuick
import QtQuick.Layouts

ColumnLayout {
    id: root
    spacing: 0

    property var date
    property int w
    property int h
    property int date_offset
    property int range
}
