version = 2

[[questions]]
question = "What is the primary difference between computer architecture and computer organization?"
choices = [
    "Architecture is visible to the programmer, organization is how features are implemented",
    "Organization is visible to the programmer, architecture is how features are implemented",
    "They are exactly the same concept",
    "Architecture deals with hardware, organization deals with software",
]
answer = "Architecture is visible to the programmer, organization is how features are implemented"
explanations = [
    "Computer architecture refers to those attributes visible to the programmer (instruction set, data types, I/O mechanisms), while computer organization refers to how these features are implemented (control signals, memory technology).",
    "",
    "",
    "",
]

[[questions]]
question = "What are the four main structural components of a computer?"
choices = [
    "CPU, RAM, ROM, Hard Drive",
    "CPU, Main Memory, I/O, System Interconnection",
    "Processor, Cache, Bus, Peripherals",
    "ALU, Control Unit, Registers, Cache",
]
answer = "CPU, Main Memory, I/O, System Interconnection"
explanations = [
    "",
    "The four main structural components are the Central Processing Unit (CPU), Main Memory, Input/Output (I/O), and System Interconnection (the mechanism for communication among them).",
    "",
    "",
]

[[questions]]
question = "In the context of multicore systems, what is the definition of a \"core\"?"
choices = [
    "The entire processor chip",
    "The main memory controller",
    "An individual processing unit on a processor chip",
    "The cache memory subsystem",
]
answer = "An individual processing unit on a processor chip"
explanations = [
    "",
    "",
    "A core is an individual processing unit on a processor chip that can execute instructions independently. A processor may contain one or multiple cores.",
    "",
]

[[questions]]
question = "What is the fundamental contribution of the IAS computer?"
choices = [
    "First computer to use integrated circuits",
    "Stored program concept",
    "First commercial computer",
    "Introduction of pipelining",
]
answer = "Stored program concept"
explanations = [
    "",
    "The IAS computer (completed in 1952) implemented the stored program concept attributed to John von Neumann, becoming the prototype for all subsequent general-purpose computers.",
    "",
    "",
]

[[questions]]
question = "The von Neumann architecture is based on three key concepts. Which of the following is NOT one of them?"
choices = [
    "Data and instructions are stored in a single read-write memory",
    "Memory contents are addressable by location",
    "Execution occurs in parallel fashion across multiple instructions",
    "Execution occurs sequentially unless explicitly modified",
]
answer = "Execution occurs in parallel fashion across multiple instructions"
explanations = [
    "",
    "",
    "The von Neumann architecture specifies sequential execution, not parallel. Instructions are fetched and executed one after another unless a branch instruction explicitly changes the sequence.",
    "",
]

[[questions]]
question = "Which statement about cache memory is TRUE?"
choices = [
    "Cache is larger and slower than main memory",
    "Cache is smaller and faster than main memory",
    "Cache is used only for permanent storage",
    "Cache replaces main memory entirely",
]
answer = "Cache is smaller and faster than main memory"
explanations = [
    "",
    "Cache memory is smaller and faster than main memory, placed between the processor and main memory to speed up memory access by storing data likely to be used in the near future.",
    "",
    "",
]

[[questions]]
question = "What distinguishes an embedded system from a general-purpose computer?"
choices = [
    "Embedded systems are always faster",
    "Embedded systems are designed for specific tasks within larger devices",
    "Embedded systems cannot use operating systems",
    "Embedded systems have no memory constraints",
]
answer = "Embedded systems are designed for specific tasks within larger devices"
explanations = [
    "",
    "Embedded systems are specialized computing systems integrated into larger devices, often with real-time constraints and tight coupling to their environment.",
    "",
    "",
]

[[questions]]
question = "What is a deeply embedded system?"
choices = [
    "A system with deep memory hierarchy",
    "A system with processor behavior difficult to observe, using microcontrollers",
    "A system with multiple processors",
    "A system that requires constant user interaction",
]
answer = "A system with processor behavior difficult to observe, using microcontrollers"
explanations = [
    "",
    "Deeply embedded systems use microcontrollers rather than microprocessors, have non-programmable logic burned into ROM, no user interaction, and extreme resource constraints.",
    "",
    "",
]

[[questions]]
question = "Which type of processor is defined by its ability to execute complex operating systems?"
choices = [
    "Dedicated processor",
    "Embedded processor",
    "Application processor",
    "Microcontroller",
]
answer = "Application processor"
explanations = [
    "",
    "",
    "Application processors are general-purpose processors designed to run complex operating systems and support numerous applications, such as those found in smartphones.",
    "",
]

[[questions]]
question = "What is the role of the Arithmetic and Logic Unit (ALU)?"
choices = [
    "Controls the operation of the CPU",
    "Stores data temporarily",
    "Performs data processing functions",
    "Manages I/O operations",
]
answer = "Performs data processing functions"
explanations = [
    "",
    "",
    "The ALU performs the computer's data processing functions, including arithmetic calculations and logical operations on data.",
    "",
]

[[questions]]
question = "According to Amdahl's Law, what limits the potential speedup of a program using multiple processors?"
choices = [
    "The number of processors available",
    "The speed of individual processors",
    "The fraction of the program that cannot be parallelized",
    "The size of the program",
]
answer = "The fraction of the program that cannot be parallelized"
explanations = [
    "",
    "",
    "Amdahl's Law states that the speedup is limited by the portion of the program that must execute sequentially. Even with infinite processors, speedup is bounded by 1/(1-P) where P is the parallelizable fraction.",
    "",
]

[[questions]]
question = "What is the formula for Amdahl's Law speedup?"
choices = [
    "Speedup = 1 / (1 - P + P/N)",
    "Speedup = P × N",
    "Speedup = N / (1 - P)",
    "Speedup = 1 / P",
]
answer = "Speedup = 1 / (1 - P + P/N)"
explanations = [
    "Amdahl's Law: Speedup = 1 / [(1 - P) + P/N] where P is the parallelizable portion and N is the number of processors. The sequential portion (1-P) always remains.",
    "",
    "",
    "",
]

[[questions]]
question = "Little's Law states that:"
choices = [
    "The average number of items in a queuing system equals arrival rate times average time in system",
    "The speedup is limited by the sequential portion of a program",
    "Power consumption is proportional to frequency squared",
    "Memory latency increases with capacity",
]
answer = "The average number of items in a queuing system equals arrival rate times average time in system"
explanations = [
    "Little's Law: L = λ × W, where L is average number of items in the system, λ is average arrival rate, and W is average time an item spends in the system.",
    "",
    "",
    "",
]

[[questions]]
question = "In SPEC CPU2017, what is the difference between a \"speed metric\" and a \"rate metric\"?"
choices = [
    "Speed measures throughput, rate measures single-task time",
    "Speed measures single-task execution time, rate measures how many tasks can be completed in a given time",
    "Speed uses integer benchmarks, rate uses floating-point",
    "Speed uses one processor, rate uses multiple processors",
]
answer = "Speed measures single-task execution time, rate measures how many tasks can be completed in a given time"
explanations = [
    "",
    "Speed metric measures the time to execute a single task (latency), while rate metric measures throughput—how many tasks can be completed in a certain time period.",
    "",
    "",
]

[[questions]]
question = "What is the primary challenge addressed by RC delay in processor design?"
choices = [
    "Power consumption in transistors",
    "The speed at which electrons flow through metal wires",
    "Heat dissipation from logic gates",
    "Memory access latency",
]
answer = "The speed at which electrons flow through metal wires"
explanations = [
    "",
    "RC delay refers to the limitation on electron flow speed due to resistance and capacitance of metal interconnects. As wires become thinner and closer together, RC delay increases.",
    "",
    "",
]

[[questions]]
question = "What is the geometric mean (GM) best suited for?"
choices = [
    "Comparing execution times directly",
    "Normalized results where ratio comparison is meaningful",
    "Measuring total throughput",
    "Calculating average power consumption",
]
answer = "Normalized results where ratio comparison is meaningful"
explanations = [
    "",
    "The geometric mean is appropriate for normalized results because it's independent of the reference machine and gives consistent relative performance comparisons.",
    "",
    "",
]

[[questions]]
question = "What does the term \"instruction execution rate\" measure?"
choices = [
    "The number of instructions in a program",
    "The number of instructions executed per second (MIPS)",
    "The number of instructions per byte",
    "The clock speed of the processor",
]
answer = "The number of instructions executed per second (MIPS)"
explanations = [
    "",
    "Instruction execution rate, typically measured in MIPS (millions of instructions per second), indicates how many instructions the processor can execute in one second.",
    "",
    "",
]

[[questions]]
question = "Which of the following is a characteristic of a good benchmark program?"
choices = [
    "Written in assembly language",
    "Not portable across machines",
    "Representative of a particular programming domain",
    "Difficult to measure",
]
answer = "Representative of a particular programming domain"
explanations = [
    "",
    "",
    "Good benchmarks should be representative of actual programming domains, written in high-level languages for portability, easy to measure, and widely distributed.",
    "",
]

[[questions]]
question = "According to the text, what is the \"base metric\" in SPEC benchmarks?"
choices = [
    "The fastest possible measurement",
    "Required results with strict compilation guidelines",
    "Measurements using specialized hardware",
    "Results from the reference machine",
]
answer = "Required results with strict compilation guidelines"
explanations = [
    "",
    "Base metrics are required for all reported results and have strict guidelines for compilation, ensuring consistent and comparable measurements.",
    "",
    "",
]

[[questions]]
question = "What is the primary advantage of using multiple levels of cache?"
choices = [
    "Reduced cost",
    "Greater performance improvement than single-level cache",
    "Simplified architecture",
    "Lower power consumption",
]
answer = "Greater performance improvement than single-level cache"
explanations = [
    "",
    "Multiple cache levels (L1, L2, L3) provide greater performance improvement by creating a hierarchy where the fastest cache handles most accesses and larger caches catch misses.",
    "",
    "",
]

[[questions]]
question = "What does IPC (instructions per cycle) measure?"
choices = [
    "Number of instructions executed per second",
    "Average number of instructions completed per clock cycle",
    "Cycles per instruction",
    "Processor clock frequency",
]
answer = "Average number of instructions completed per clock cycle"
explanations = [
    "",
    "IPC measures the average number of instructions completed per clock cycle, reflecting processor efficiency independent of clock speed.",
    "",
    "",
]

[[questions]]
question = "Why is power density a problem in modern processors?"
choices = [
    "It reduces processing speed",
    "Heat dissipation becomes difficult as logic density and clock speed increase",
    "It increases memory latency",
    "It causes RC delay",
]
answer = "Heat dissipation becomes difficult as logic density and clock speed increase"
explanations = [
    "",
    "As logic density and clock speed increase, power density increases, making heat dissipation challenging and potentially damaging components without adequate cooling.",
    "",
    "",
]

[[questions]]
question = "What does the Program Counter (PC) hold?"
choices = [
    "The current instruction being executed",
    "The address of the next instruction to be fetched",
    "The result of the last arithmetic operation",
    "The memory address of data",
]
answer = "The address of the next instruction to be fetched"
explanations = [
    "",
    "The Program Counter (PC) holds the address of the next instruction to be fetched. After each instruction fetch, the PC is incremented to point to the next sequential instruction.",
    "",
    "",
]

[[questions]]
question = "What is Direct Memory Access (DMA)?"
choices = [
    "Processor directly accessing memory without cache",
    "I/O module exchanging data directly with memory without processor involvement",
    "Memory accessing I/O devices directly",
    "Cache directly accessing main memory",
]
answer = "I/O module exchanging data directly with memory without processor involvement"
explanations = [
    "",
    "DMA allows I/O modules to read from or write to memory directly, without tying up the processor. The processor grants authority to the I/O module for memory transfers.",
    "",
    "",
]

[[questions]]
question = "What determines the maximum possible memory capacity in a system?"
choices = [
    "Data bus width",
    "Control bus width",
    "Address bus width",
    "Processor clock speed",
]
answer = "Address bus width"
explanations = [
    "",
    "",
    "The width of the address bus determines the number of unique addresses that can be referenced, thus determining the maximum possible memory capacity (2^address lines).",
    "",
]

[[questions]]
question = "What is the function of the data bus?"
choices = [
    "To designate the source or destination of data",
    "To provide a path for moving data among system modules",
    "To control access to data and address lines",
    "To provide timing signals",
]
answer = "To provide a path for moving data among system modules"
explanations = [
    "",
    "The data bus provides the path for moving data among system modules. Its width determines how many bits can be transferred simultaneously.",
    "",
    "",
]

[[questions]]
question = "What is QuickPath Interconnect (QPI)?"
choices = [
    "A shared bus architecture",
    "A point-to-point interconnect with layered protocol architecture",
    "A type of main memory",
    "An I/O interface standard",
]
answer = "A point-to-point interconnect with layered protocol architecture"
explanations = [
    "",
    "QPI is a point-to-point interconnect introduced in 2008 that uses direct pairwise connections, eliminating arbitration, with a layered protocol architecture and packetized data transfer.",
    "",
    "",
]

[[questions]]
question = "In PCI Express, what is a \"split transaction\"?"
choices = [
    "A transaction split across multiple buses",
    "A request packet sent with a response (completion packet) expected",
    "A transaction that requires multiple clock cycles",
    "A transaction split between memory and I/O",
]
answer = "A request packet sent with a response (completion packet) expected"
explanations = [
    "",
    "Split transactions involve sending a request packet and waiting for a completion packet response, allowing other transactions to proceed while waiting.",
    "",
    "",
]

[[questions]]
question = "What are the four address spaces supported by PCI Express Transaction Layer?"
choices = [
    "Memory, I/O, Configuration, Message",
    "Data, Instruction, Control, Status",
    "Local, Remote, Cache, Main",
    "Input, Output, Storage, Processing",
]
answer = "Memory, I/O, Configuration, Message"
explanations = [
    "PCIe supports four address spaces: Memory (main memory and I/O devices), Configuration (I/O device registers), Message (control signals for interrupts, error handling), and I/O (legacy PCI devices).",
    "",
    "",
    "",
]

[[questions]]
question = "What is the purpose of the Control Bus?"
choices = [
    "To move data between modules",
    "To designate source or destination of data",
    "To transmit command and timing information",
    "To provide power to modules",
]
answer = "To transmit command and timing information"
explanations = [
    "",
    "",
    "The control bus transmits command and timing information, including signals that indicate validity of data and address information and specify operations to be performed.",
    "",
]

[[questions]]
question = "What is the function of the Instruction Register (IR)?"
choices = [
    "Hold the address of the next instruction",
    "Hold the instruction most recently fetched",
    "Hold data being written to memory",
    "Hold the memory address for I/O",
]
answer = "Hold the instruction most recently fetched"
explanations = [
    "",
    "The Instruction Register (IR) contains the instruction most recently fetched from memory, holding it while the control unit decodes and executes it.",
    "",
    "",
]

[[questions]]
question = "In the indirect cycle, what happens when an instruction specifies indirect addressing?"
choices = [
    "The operand is fetched directly",
    "The address field is used to fetch the actual operand address",
    "The instruction is executed immediately",
    "The instruction is ignored",
]
answer = "The address field is used to fetch the actual operand address"
explanations = [
    "",
    "In indirect addressing, the address field contains the address of a memory location that holds the actual operand address. The indirect cycle fetches this effective address.",
    "",
    "",
]

[[questions]]
question = "What is the principle of locality (locality of reference)?"
choices = [
    "Memory references are evenly distributed throughout execution",
    "Memory references by the processor tend to cluster during execution",
    "All memory locations are accessed equally often",
    "Programs access random memory locations",
]
answer = "Memory references by the processor tend to cluster during execution"
explanations = [
    "",
    "Locality of reference reflects the observation that during program execution, memory references tend to cluster, with programs repeatedly accessing the same or nearby memory locations.",
    "",
    "",
]

[[questions]]
question = "What is temporal locality?"
choices = [
    "Accessing memory locations with nearby addresses",
    "Referencing memory units accessed in the recent past",
    "Accessing memory in sequential order",
    "Accessing memory at specific times",
]
answer = "Referencing memory units accessed in the recent past"
explanations = [
    "",
    "Temporal locality refers to the tendency to reference in the near future memory units that were referenced in the recent past, such as loops and frequently accessed variables.",
    "",
    "",
]

[[questions]]
question = "What is spatial locality?"
choices = [
    "Accessing the same memory location repeatedly",
    "Accessing memory units whose addresses are near one another",
    "Accessing memory at specific spatial coordinates",
    "Random memory access patterns",
]
answer = "Accessing memory units whose addresses are near one another"
explanations = [
    "",
    "Spatial locality refers to the tendency to reference memory units with addresses near each other, such as when processing sequential data in arrays or executing sequential instructions.",
    "",
    "",
]

[[questions]]
question = "What trade-off exists in memory hierarchy design?"
choices = [
    "Faster access time, greater cost per bit; greater capacity, slower access time",
    "Faster access time, lower cost per bit",
    "Larger capacity, faster access time",
    "All memory types have identical characteristics",
]
answer = "Faster access time, greater cost per bit; greater capacity, slower access time"
explanations = [
    "There is an inherent trade-off: faster access memory costs more per bit and typically has less capacity, while larger capacity memory is slower and costs less per bit.",
    "",
    "",
    "",
]

[[questions]]
question = "In a two-level memory system, what is the purpose of the upper-level memory (M1)?"
choices = [
    "To permanently store all data",
    "To serve as temporary store for part of the larger M2's contents",
    "To replace the lower-level memory",
    "To store only system programs",
]
answer = "To serve as temporary store for part of the larger M2's contents"
explanations = [
    "",
    "Upper-level memory (M1) is smaller, faster, and more expensive, serving as a temporary store for frequently accessed portions of the larger, slower M2 memory.",
    "",
    "",
]

[[questions]]
question = "What is the unit of transfer for internal memory?"
choices = [
    "Always one byte",
    "The number of electrical lines into and out of the memory module",
    "One word",
    "Variable based on instruction",
]
answer = "The number of electrical lines into and out of the memory module"
explanations = [
    "",
    "The unit of transfer for internal memory equals the number of electrical lines into and out of the memory module, determining how many bits can be read or written simultaneously.",
    "",
    "",
]

[[questions]]
question = "What is a \"block\" in the context of memory hierarchy?"
choices = [
    "A single byte of data",
    "The minimum unit of transfer between levels of memory",
    "A sector on disk",
    "A cache line tag",
]
answer = "The minimum unit of transfer between levels of memory"
explanations = [
    "",
    "A block is the minimum unit of information that can be transferred between levels of the memory hierarchy, such as between cache and main memory.",
    "",
    "",
]

[[questions]]
question = "What is a \"cache miss\"?"
choices = [
    "When data is found in cache",
    "When data is not found in cache and must be fetched from lower level",
    "When cache is full",
    "When processor cannot access cache",
]
answer = "When data is not found in cache and must be fetched from lower level"
explanations = [
    "",
    "A cache miss occurs when the requested data is not found in the cache, requiring the block to be fetched from a lower level of memory hierarchy.",
    "",
    "",
]

[[questions]]
question = "What is a cache line?"
choices = [
    "A portion of main memory",
    "A portion of cache memory capable of holding one block",
    "The connection between cache and processor",
    "The tag portion of cache",
]
answer = "A portion of cache memory capable of holding one block"
explanations = [
    "",
    "A line is a portion of cache memory that can hold one block of data, typically drawn as a horizontal unit in cache diagrams.",
    "",
    "",
]

[[questions]]
question = "What is the purpose of a cache tag?"
choices = [
    "To store data values",
    "A portion of a cache line used for addressing purposes",
    "To mark dirty data",
    "To indicate cache size",
]
answer = "A portion of a cache line used for addressing purposes"
explanations = [
    "",
    "The tag is a portion of a cache line used for addressing purposes, containing part of the memory address to identify which block is stored in that line.",
    "",
    "",
]

[[questions]]
question = "In direct-mapped cache, where can a given main memory block be placed?"
choices = [
    "Anywhere in the cache",
    "In exactly one specific cache line",
    "In any line of a specific set",
    "In a location determined by the replacement algorithm",
]
answer = "In exactly one specific cache line"
explanations = [
    "",
    "In direct mapping, each main memory block maps to exactly one specific cache line, determined by the block address modulo the number of cache lines.",
    "",
    "",
]

[[questions]]
question = "What is Content-Addressable Memory (CAM)?"
choices = [
    "Memory that uses addresses to locate data",
    "Memory that searches its entire contents in parallel for a match",
    "Memory that stores content with addresses",
    "Memory with fixed content",
]
answer = "Memory that searches its entire contents in parallel for a match"
explanations = [
    "",
    "CAM (associative memory) searches its entire memory in parallel for a supplied bit string, returning the address and associated data in one clock cycle.",
    "",
    "",
]

[[questions]]
question = "In set-associative mapping, how does a block map to cache?"
choices = [
    "To any line in any set",
    "To a specific line in a specific set",
    "To any line in a specific set",
    "To a specific line in any set",
]
answer = "To any line in a specific set"
explanations = [
    "",
    "",
    "In set-associative mapping, each block maps to any line within a specific set, combining flexibility with reduced search complexity.",
    "",
]

[[questions]]
question = "Which replacement algorithm is most effective and commonly used?"
choices = [
    "First-in-first-out (FIFO)",
    "Least frequently used (LFU)",
    "Least recently used (LRU)",
    "Random replacement",
]
answer = "Least recently used (LRU)"
explanations = [
    "",
    "",
    "LRU (Least Recently Used) is the most effective and popular replacement algorithm, replacing the block that has been in the cache longest without being referenced.",
    "",
]

[[questions]]
question = "What is the difference between write-through and write-back cache policies?"
choices = [
    "Write-through writes only to cache; write-back writes to both",
    "Write-through writes to both cache and main memory; write-back writes only to cache initially",
    "Write-through is faster; write-back is slower",
    "Write-through uses DMA; write-back uses programmed I/O",
]
answer = "Write-through writes to both cache and main memory; write-back writes only to cache initially"
explanations = [
    "",
    "Write-through updates both cache and main memory simultaneously. Write-back updates only the cache, marking the line as \"dirty\" for later write to main memory.",
    "",
    "",
]

[[questions]]
question = "What problem does cache coherency address in multiprocessor systems?"
choices = [
    "Caches being too small",
    "Different caches containing inconsistent copies of the same data",
    "Cache access speed",
    "Cache replacement algorithms",
]
answer = "Different caches containing inconsistent copies of the same data"
explanations = [
    "",
    "Cache coherency ensures that when one processor updates data in its cache, other caches that contain the same data are either updated or invalidated to maintain consistency.",
    "",
    "",
]

[[questions]]
question = "What is a split cache?"
choices = [
    "Cache split between processors",
    "Separate caches for instructions and data",
    "Cache with multiple levels",
    "Cache split between main memory and disk",
]
answer = "Separate caches for instructions and data"
explanations = [
    "",
    "A split cache uses separate dedicated caches for instructions and data, typically at the L1 level, eliminating contention between instruction fetch and data access.",
    "",
    "",
]

[[questions]]
question = "What is the inclusive cache policy?"
choices = [
    "Data in one cache is guaranteed not to be in lower levels",
    "Data in one cache is guaranteed to be found in all lower levels",
    "Data may or may not be in lower levels",
    "Data is never duplicated",
]
answer = "Data in one cache is guaranteed to be found in all lower levels"
explanations = [
    "",
    "",
    "",
    "",
]

[[questions]]
question = "What is the primary advantage of an on-chip cache?"
choices = [
    "Lower cost than off-chip cache",
    "Reduces processor's external bus activity and speeds execution",
    "Increases cache size",
    "Simplifies cache coherency",
]
answer = "Reduces processor's external bus activity and speeds execution"
explanations = [
    "",
    "On-chip cache reduces external bus activity since frequently accessed data is found within the processor chip, speeding execution and freeing the bus for other transfers.",
    "",
    "",
]

[[questions]]
question = "What is the \"write allocate\" policy?"
choices = [
    "Writing directly to main memory",
    "When a write miss occurs, the block is fetched into cache before the write",
    "Writing only to cache without allocating new line",
    "Allocating memory for cache writes",
]
answer = "When a write miss occurs, the block is fetched into cache before the write"
explanations = [
    "",
    "Write allocate fetches the block containing the target word from main memory into the cache on a write miss, then performs the write in the cache.",
    "",
    "",
]

[[questions]]
question = "Which cache timing model allows for simple and fast speculation?"
choices = [
    "Fully associative",
    "Set associative",
    "Direct-mapped",
    "All of the above equally",
]
answer = "Direct-mapped"
explanations = [
    "",
    "",
    "Direct-mapped cache allows speculation because the line number is known before the tag comparison, enabling parallel data access and tag check.",
    "",
]

[[questions]]
question = "What is the main disadvantage of a unified cache compared to split caches?"
choices = [
    "Higher miss rate",
    "Contention between instruction fetch and data access",
    "More complex design",
    "Higher power consumption",
]
answer = "Contention between instruction fetch and data access"
explanations = [
    "",
    "A unified cache can create contention when both instruction fetch and data access try to use the same cache simultaneously, potentially causing pipeline stalls.",
    "",
    "",
]

[[questions]]
question = "What is the purpose of a \"dirty bit\" in cache design?"
choices = [
    "To indicate the cache line needs replacement",
    "To indicate the line has been modified and differs from main memory",
    "To indicate the line is frequently accessed",
    "To mark the line as read-only",
]
answer = "To indicate the line has been modified and differs from main memory"
explanations = [
    "",
    "A dirty bit indicates that the cache line has been modified (written to) and is not consistent with main memory, requiring write-back when replaced.",
    "",
    "",
]

[[questions]]
question = "What is a \"victim cache\"?"
choices = [
    "A cache that stores data from failed operations",
    "A small cache holding recently evicted lines to reduce conflict misses",
    "A cache used only for instructions",
    "A cache that never experiences misses",
]
answer = "A small cache holding recently evicted lines to reduce conflict misses"
explanations = [
    "",
    "A victim cache is a small, fully associative cache that holds recently evicted cache lines, reducing conflict misses by capturing lines that would otherwise be lost.",
    "",
    "",
]

[[questions]]
question = "What is the relationship between line size and spatial locality?"
choices = [
    "Smaller line sizes better exploit spatial locality",
    "Larger line sizes better exploit spatial locality",
    "Line size has no effect on spatial locality",
    "Only temporal locality matters",
]
answer = "Larger line sizes better exploit spatial locality"
explanations = [
    "",
    "Larger line sizes take advantage of spatial locality by bringing in neighboring data that is likely to be accessed soon, reducing compulsory misses.",
    "",
    "",
]

[[questions]]
question = "What is the fundamental difference between DRAM and SRAM?"
choices = [
    "DRAM uses flip-flops, SRAM uses capacitors",
    "DRAM uses capacitors requiring refresh, SRAM uses flip-flops",
    "DRAM is faster than SRAM",
    "DRAM is nonvolatile, SRAM is volatile",
]
answer = "DRAM uses capacitors requiring refresh, SRAM uses flip-flops"
explanations = [
    "",
    "DRAM stores data as charge on capacitors and requires periodic refresh; SRAM uses flip-flop logic gates and retains data as long as power is supplied without refresh.",
    "",
    "",
]

[[questions]]
question = "Which memory type is typically used for cache memory?"
choices = [
    "DRAM",
    "SRAM",
    "Flash memory",
    "ROM",
]
answer = "SRAM"
explanations = [
    "",
    "SRAM is faster than DRAM and is typically used for cache memory, both on-chip and off-chip, due to its speed and no refresh requirement.",
    "",
    "",
]

[[questions]]
question = "What is the primary advantage of DRAM over SRAM?"
choices = [
    "Faster access speed",
    "Higher density and lower cost per bit",
    "No refresh required",
    "Lower power consumption",
]
answer = "Higher density and lower cost per bit"
explanations = [
    "",
    "DRAM cells are simpler and smaller than SRAM cells, allowing higher density (more bits per chip) and lower cost per bit, making it suitable for main memory.",
    "",
    "",
]

[[questions]]
question = "What is a \"soft error\" in memory?"
choices = [
    "A permanent physical defect",
    "A random, non-destructive event that alters memory contents",
    "A manufacturing defect",
    "A software programming error",
]
answer = "A random, non-destructive event that alters memory contents"
explanations = [
    "",
    "Soft errors are random events (caused by alpha particles, power supply issues) that alter memory contents without permanent damage to the memory cells.",
    "",
    "",
]

[[questions]]
question = "What is Hamming code used for in memory systems?"
choices = [
    "Increasing memory speed",
    "Error detection and correction",
    "Data compression",
    "Address translation",
]
answer = "Error detection and correction"
explanations = [
    "",
    "Hamming code is an error-correcting code that can detect and correct single-bit errors and detect double-bit errors (SEC-DED) in memory systems.",
    "",
    "",
]

[[questions]]
question = "What is the key feature of Synchronous DRAM (SDRAM)?"
choices = [
    "Asynchronous operation with memory bus",
    "Synchronized with processor clock for pipelined access",
    "Uses double data rate",
    "Nonvolatile operation",
]
answer = "Synchronized with processor clock for pipelined access"
explanations = [
    "",
    "SDRAM synchronizes its operations with the processor clock, allowing pipelined access where new commands can be issued while previous data is being read.",
    "",
    "",
]

[[questions]]
question = "How does DDR SDRAM achieve higher data rates?"
choices = [
    "Only by increasing clock frequency",
    "By transferring data on both rising and falling clock edges",
    "By using wider data buses",
    "By reducing latency",
]
answer = "By transferring data on both rising and falling clock edges"
explanations = [
    "",
    "DDR SDRAM transfers data on both the rising and falling edges of the clock, effectively doubling the data transfer rate without doubling the clock frequency.",
    "",
    "",
]

[[questions]]
question = "What is eDRAM (embedded DRAM)?"
choices = [
    "DRAM used for external storage",
    "DRAM integrated on the same chip as the processor or ASIC",
    "DRAM with error correction",
    "DRAM used only for graphics",
]
answer = "DRAM integrated on the same chip as the processor or ASIC"
explanations = [
    "",
    "eDRAM is DRAM integrated directly onto the same chip or MCM as the processor or ASIC, providing intermediate characteristics between SRAM and off-chip DRAM.",
    "",
    "",
]

[[questions]]
question = "Which of the following is true about Flash memory?"
choices = [
    "Provides byte-level erasure like EEPROM",
    "Uses only one transistor per bit and erases blocks of memory",
    "Is faster than DRAM",
    "Requires constant power to retain data",
]
answer = "Uses only one transistor per bit and erases blocks of memory"
explanations = [
    "",
    "Flash memory uses one transistor per bit (like EPROM) but erases blocks of memory rather than bytes, combining density with electrical erasability.",
    "",
    "",
]

[[questions]]
question = "What is the difference between NOR and NAND flash memory?"
choices = [
    "NOR is serial access, NAND is random access",
    "NOR provides random access, NAND provides serial access with higher density",
    "NOR is slower than NAND",
    "NOR is nonvolatile, NAND is volatile",
]
answer = "NOR provides random access, NAND provides serial access with higher density"
explanations = [
    "",
    "NOR flash provides random access suitable for code storage; NAND flash offers higher density and lower cost with serial access, better for data storage.",
    "",
    "",
]

[[questions]]
question = "What is interleaved memory?"
choices = [
    "Memory with error correction interleaved",
    "Memory organized across multiple banks for parallel access",
    "Memory interleaved with cache",
    "Memory with interleaved addressing",
]
answer = "Memory organized across multiple banks for parallel access"
explanations = [
    "",
    "Interleaved memory uses multiple memory banks that can be accessed in parallel, allowing consecutive addresses to be spread across banks for faster access.",
    "",
    "",
]

[[questions]]
question = "What is a \"hard failure\" in memory?"
choices = [
    "A soft error that becomes permanent",
    "A permanent physical defect that affects memory cells",
    "A failure in the memory controller",
    "A software crash",
]
answer = "A permanent physical defect that affects memory cells"
explanations = [
    "",
    "Hard failure is a permanent physical defect that causes affected memory cells to become stuck at 0 or 1 or switch erratically, caused by manufacturing defects, wear, or environmental abuse.",
    "",
    "",
]

[[questions]]
question = "What is seek time in disk performance?"
choices = [
    "Time for disk to spin to correct sector",
    "Time to position the head at the desired track",
    "Time to transfer data",
    "Total access time",
]
answer = "Time to position the head at the desired track"
explanations = [
    "",
    "Seek time is the time required to position the read-write head over the desired track on a movable-head disk system.",
    "",
    "",
]

[[questions]]
question = "What is rotational delay (latency) in disk access?"
choices = [
    "Time to move the head to the track",
    "Time for the desired sector to rotate under the head",
    "Time to read the data",
    "Time for the disk to start spinning",
]
answer = "Time for the desired sector to rotate under the head"
explanations = [
    "",
    "Rotational delay (latency) is the time waiting for the beginning of the desired sector to rotate to the position of the read-write head.",
    "",
    "",
]

[[questions]]
question = "What is the total access time for a disk I/O operation?"
choices = [
    "Seek time only",
    "Rotational delay only",
    "Seek time + Rotational delay + Transfer time",
    "Transfer time only",
]
answer = "Seek time + Rotational delay + Transfer time"
explanations = [
    "",
    "",
    "Disk access time is the sum of seek time (head positioning), rotational latency (sector positioning), and transfer time (data read/write).",
    "",
]

[[questions]]
question = "What is RAID (Redundant Array of Independent Disks)?"
choices = [
    "A single disk with redundancy",
    "A set of physical disks viewed as a single logical drive with data distribution",
    "A type of disk controller",
    "A backup software",
]
answer = "A set of physical disks viewed as a single logical drive with data distribution"
explanations = [
    "",
    "RAID combines multiple physical disks into a single logical drive, distributing data across the array with redundant capacity for data recovery.",
    "",
    "",
]

[[questions]]
question = "Which RAID level uses striping without redundancy?"
choices = [
    "RAID 0",
    "RAID 1",
    "RAID 5",
    "RAID 6",
]
answer = "RAID 0"
explanations = [
    "RAID 0 uses striping across disks without any redundancy, providing performance benefits but no fault tolerance—any disk failure results in data loss.",
    "",
    "",
    "",
]

[[questions]]
question = "What is a key characteristic of RAID 1?"
choices = [
    "Striping with parity",
    "Mirroring (duplicating all data)",
    "Bit-level striping with Hamming code",
    "Distributed parity",
]
answer = "Mirroring (duplicating all data)"
explanations = [
    "",
    "RAID 1 uses disk mirroring where each logical strip is mapped to two separate physical disks, providing complete data duplication for redundancy.",
    "",
    "",
]

[[questions]]
question = "How does RAID 5 differ from RAID 4?"
choices = [
    "RAID 5 uses more parity disks",
    "RAID 5 distributes parity strips across all disks",
    "RAID 5 uses mirroring",
    "RAID 5 has no parity",
]
answer = "RAID 5 distributes parity strips across all disks"
explanations = [
    "",
    "RAID 5 distributes parity blocks across all disks in a round-robin fashion, eliminating the dedicated parity disk bottleneck found in RAID 4.",
    "",
    "",
]

[[questions]]
question = "What is the advantage of SSD over HDD?"
choices = [
    "Lower cost per GB",
    "Higher IOPS, lower access times, lower power consumption",
    "Larger capacity",
    "Unlimited write cycles",
]
answer = "Higher IOPS, lower access times, lower power consumption"
explanations = [
    "",
    "SSDs offer higher input/output operations per second (IOPS), lower access times, lower power consumption, and better durability compared to HDDs.",
    "",
    "",
]

[[questions]]
question = "What are the four types of I/O commands an I/O module may receive?"
choices = [
    "Start, Stop, Read, Write",
    "Control, Test, Read, Write",
    "Initiate, Execute, Complete, Stop",
    "Open, Close, Read, Write",
]
answer = "Control, Test, Read, Write"
explanations = [
    "",
    "The four I/O commands are: Control (activate peripheral), Test (check status), Read (obtain data from peripheral), Write (send data to peripheral).",
    "",
    "",
]

[[questions]]
question = "What is programmed I/O?"
choices = [
    "Processor issues command and continues execution",
    "Processor issues command and waits until I/O is complete",
    "I/O module has its own processor",
    "I/O operations are handled by DMA",
]
answer = "Processor issues command and waits until I/O is complete"
explanations = [
    "",
    "In programmed I/O, the processor issues an I/O command and then actively waits (polls) until the operation completes, wasting processor cycles.",
    "",
    "",
]

[[questions]]
question = "What is the main advantage of interrupt-driven I/O over programmed I/O?"
choices = [
    "Simpler hardware",
    "Processor can execute other instructions while waiting for I/O",
    "Higher data transfer rates",
    "Lower cost",
]
answer = "Processor can execute other instructions while waiting for I/O"
explanations = [
    "",
    "Interrupt-driven I/O allows the processor to continue executing other instructions while the I/O operation is in progress, improving processor utilization.",
    "",
    "",
]

[[questions]]
question = "What is the difference between memory-mapped I/O and isolated I/O?"
choices = [
    "Memory-mapped uses separate address space; isolated shares memory address space",
    "Memory-mapped shares memory address space; isolated uses separate I/O address space",
    "Memory-mapped uses DMA; isolated uses programmed I/O",
    "Memory-mapped is slower; isolated is faster",
]
answer = "Memory-mapped shares memory address space; isolated uses separate I/O address space"
explanations = [
    "",
    "Memory-mapped I/O uses the same address space for memory and I/O devices, while isolated I/O uses separate address spaces with special I/O instructions.",
    "",
    "",
]

[[questions]]
question = "What is vectored interrupt?"
choices = [
    "Interrupts are processed in a vector (array)",
    "Processor uses vector from interrupting device to directly access service routine",
    "Interrupts are disabled",
    "Multiple interrupts are queued",
]
answer = "Processor uses vector from interrupting device to directly access service routine"
explanations = [
    "",
    "Vectored interrupt uses a unique identifier (vector) from the interrupting device that points directly to the appropriate interrupt service routine, eliminating the need for a general poll.",
    "",
    "",
]

[[questions]]
question = "What is the primary benefit of Direct Memory Access (DMA)?"
choices = [
    "Reduces processor involvement in data transfer",
    "Increases I/O speed",
    "Simplifies I/O programming",
    "Reduces memory usage",
]
answer = "Reduces processor involvement in data transfer"
explanations = [
    "DMA allows I/O modules to transfer data directly to/from memory without processor involvement during the transfer, freeing the processor for other tasks.",
    "",
    "",
    "",
]

[[questions]]
question = "What is Direct Cache Access (DCA)?"
choices = [
    "DMA transfers directly to cache instead of main memory",
    "Processor accesses cache directly",
    "I/O devices access cache directly",
    "Cache bypasses main memory",
]
answer = "DMA transfers directly to cache instead of main memory"
explanations = [
    "DCA is a technique where I/O data is transferred directly into the processor's cache rather than main memory, reducing latency and memory bandwidth usage.",
    "",
    "",
    "",
]

[[questions]]
question = "What data rates are supported by USB 3.0 SuperSpeed?"
choices = [
    "480 Mbps",
    "5 Gbps signaling rate",
    "10 Gbps",
    "12 Mbps",
]
answer = "5 Gbps signaling rate"
explanations = [
    "",
    "USB 3.0 SuperSpeed provides a signaling rate of 5 Gbps, with usable data rates up to about 4 Gbps after overhead.",
    "",
    "",
]

[[questions]]
question = "What hardware features are required for a multiprogramming OS?"
choices = [
    "Only a fast processor",
    "Memory protection, timer, privileged instructions, interrupts",
    "Large memory only",
    "Fast I/O only",
]
answer = "Memory protection, timer, privileged instructions, interrupts"
explanations = [
    "",
    "Multiprogramming requires memory protection (prevent user program from altering monitor), timer (prevent job monopolizing system), privileged instructions (control I/O), and interrupts (flexible control transfer).",
    "",
    "",
]

[[questions]]
question = "What is the primary benefit of multiprogramming?"
choices = [
    "Simpler OS design",
    "Better resource utilization by keeping processor busy",
    "Faster program execution",
    "Less memory required",
]
answer = "Better resource utilization by keeping processor busy"
explanations = [
    "",
    "Multiprogramming improves resource utilization by allowing multiple programs to reside in memory, so the processor can switch to another program when one is waiting for I/O.",
    "",
    "",
]

[[questions]]
question = "What is time sharing?"
choices = [
    "Programs share time on the processor",
    "Processor time is shared among multiple users interacting directly",
    "Multiple processors share time",
    "Time-based scheduling only",
]
answer = "Processor time is shared among multiple users interacting directly"
explanations = [
    "",
    "Time sharing allows multiple users to simultaneously access the system through terminals, with the OS interleaving execution of each user program in short quanta.",
    "",
    "",
]

[[questions]]
question = "What are the three types of scheduling in operating systems?"
choices = [
    "Process, thread, I/O",
    "Long-term, medium-term, short-term",
    "Priority, round-robin, FIFO",
    "User, system, idle",
]
answer = "Long-term, medium-term, short-term"
explanations = [
    "",
    "The three types are: long-term (admission to system), medium-term (swapping), and short-term (dispatcher - which job executes next).",
    "",
    "",
]

[[questions]]
question = "What is swapping in memory management?"
choices = [
    "Exchanging data between CPU and memory",
    "Moving entire processes between main memory and secondary storage",
    "Swapping register contents",
    "Exchanging data between caches",
]
answer = "Moving entire processes between main memory and secondary storage"
explanations = [
    "",
    "Swapping moves entire processes between main memory and secondary storage to manage the degree of multiprogramming and free memory for other processes.",
    "",
    "",
]

[[questions]]
question = "What is the difference between logical address and physical address?"
choices = [
    "Logical address is actual location; physical is relative",
    "Logical address is relative to program start; physical is actual memory location",
    "They are the same",
    "Logical is used by hardware; physical by software",
]
answer = "Logical address is relative to program start; physical is actual memory location"
explanations = [
    "",
    "Logical (virtual) addresses are expressed relative to the beginning of the program; physical addresses are the actual locations in main memory after address translation.",
    "",
    "",
]

[[questions]]
question = "What is demand paging?"
choices = [
    "Loading entire program before execution",
    "Loading pages only when they are needed (on page fault)",
    "Preloading all pages",
    "Paging without virtual memory",
]
answer = "Loading pages only when they are needed (on page fault)"
explanations = [
    "",
    "Demand paging brings each page into memory only when it is referenced, reducing memory usage by loading only pages actually needed during execution.",
    "",
    "",
]

[[questions]]
question = "What is thrashing in virtual memory systems?"
choices = [
    "High cache hit rate",
    "Processor spends most time swapping pages instead of executing instructions",
    "Fast page access",
    "Efficient memory usage",
]
answer = "Processor spends most time swapping pages instead of executing instructions"
explanations = [
    "",
    "Thrashing occurs when the system spends more time swapping pages in and out than executing instructions, severely degrading performance.",
    "",
    "",
]

[[questions]]
question = "What is a Translation Lookaside Buffer (TLB)?"
choices = [
    "A cache for disk blocks",
    "A cache for page table entries",
    "A buffer for I/O operations",
    "A register for program counter",
]
answer = "A cache for page table entries"
explanations = [
    "",
    "TLB is a cache that stores recently used page table entries, speeding up virtual-to-physical address translation by avoiding full page table walks.",
    "",
    "",
]

[[questions]]
question = "What is segmentation in memory management?"
choices = [
    "Dividing memory into fixed-size blocks",
    "Viewing memory as multiple logical address spaces (segments)",
    "Paging with variable page sizes",
    "Dividing program into equal sections",
]
answer = "Viewing memory as multiple logical address spaces (segments)"
explanations = [
    "",
    "Segmentation allows the programmer to view memory as multiple logical address spaces or segments, simplifying program organization and data structure handling.",
    "",
    "",
]

[[questions]]
question = "Convert binary 1011.101 to decimal."
choices = [
    "11.625",
    "11.5",
    "13.625",
    "13.5",
]
answer = "11.625"
explanations = [
    "1011.101₂ = (1×8)+(0×4)+(1×2)+(1×1) + (1×0.5)+(0×0.25)+(1×0.125) = 8+2+1+0.5+0.125 = 11.625.",
    "",
    "",
    "",
]

[[questions]]
question = "What is the process for converting a decimal integer to binary?"
choices = [
    "Repeated multiplication by 2",
    "Repeated division by 2, collecting remainders",
    "Repeated subtraction of powers of 2",
    "Direct conversion using a table",
]
answer = "Repeated division by 2, collecting remainders"
explanations = [
    "",
    "Decimal to binary conversion uses repeated division by 2, collecting remainders from least significant to most significant bit. The final quotient becomes the most significant bit.",
    "",
    "",
]

[[questions]]
question = "What is the hexadecimal equivalent of binary 1110 1011?"
choices = [
    "EB",
    "E7",
    "1E",
    "BE",
]
answer = "EB"
explanations = [
    "Group binary into nibbles: 1110₂ = E₁₆, 1011₂ = B₁₆, so EB₁₆. Option B (E7) would be 1110 0111, C (1E) is 0001 1110, D (BE) is 1011 1110.",
    "",
    "",
    "",
]

[[questions]]
question = "Convert hexadecimal 2C to decimal."
choices = [
    "28",
    "44",
    "52",
    "36",
]
answer = "44"
explanations = [
    "",
    "2C₁₆ = (2 × 16¹) + (12 × 16⁰) = 32 + 12 = 44 decimal.",
    "",
    "",
]

[[questions]]
question = "In positional number systems, what does the radix point separate?"
choices = [
    "Integer and fractional parts",
    "Positive and negative numbers",
    "Most and least significant digits",
    "Base and exponent",
]
answer = "Integer and fractional parts"
explanations = [
    "The radix point (decimal point in base 10) separates the integer part (positive powers of the radix) from the fractional part (negative powers of the radix).",
    "",
    "",
    "",
]

[[questions]]
question = "What is a nibble?"
choices = [
    "2 bits",
    "4 bits",
    "8 bits",
    "16 bits",
]
answer = "4 bits"
explanations = [
    "",
    "A nibble (or nybble) is a group of 4 bits, which can represent 16 possible values and corresponds to one hexadecimal digit.",
    "",
    "",
]

[[questions]]
question = "In sign-magnitude representation, what does the most significant bit (MSB) represent?"
choices = [
    "The magnitude of the number",
    "The sign of the number (0 for positive, 1 for negative)",
    "The exponent",
    "The least significant digit",
]
answer = "The sign of the number (0 for positive, 1 for negative)"
explanations = [
    "",
    "In sign-magnitude representation, the MSB indicates the sign (0 = positive, 1 = negative), and the remaining bits represent the magnitude.",
    "",
    "",
]

[[questions]]
question = "What is the range of a 4-bit two's complement integer?"
choices = [
    "-7 to +7",
    "-8 to +7",
    "-7 to +8",
    "-8 to +8",
]
answer = "-8 to +7"
explanations = [
    "",
    "4-bit two's complement ranges from -2³ = -8 to +2³-1 = +7. Only one representation for zero, giving one more negative value than positive.",
    "",
    "",
]

[[questions]]
question = "What is the two's complement negation procedure?"
choices = [
    "Complement each bit and add 1",
    "Subtract from zero",
    "Add 1 and complement",
    "Complement each bit",
]
answer = "Complement each bit and add 1"
explanations = [
    "To negate a two's complement number, take the bitwise complement (flip all bits) and add 1 to the result.",
    "",
    "",
    "",
]

[[questions]]
question = "In two's complement addition, when does overflow occur?"
choices = [
    "When both operands are positive",
    "When result has opposite sign to both operands when they have the same sign",
    "When carry out of MSB occurs",
    "When sum exceeds 16 bits",
]
answer = "When result has opposite sign to both operands when they have the same sign"
explanations = [
    "",
    "Overflow occurs when adding two numbers of the same sign produces a result of the opposite sign, indicating the result cannot be represented in the available bits.",
    "",
    "",
]

[[questions]]
question = "What is the subtraction rule for two's complement?"
choices = [
    "Add the subtrahend directly",
    "Take two's complement of subtrahend and add to minuend",
    "Subtract directly using borrow",
    "Complement minuend and add",
]
answer = "Take two's complement of subtrahend and add to minuend"
explanations = [
    "",
    "To subtract S from M, take the two's complement of S (negate it) and add to M: M - S = M + (-S) in two's complement.",
    "",
    "",
]

[[questions]]
question = "What is the Booth's algorithm used for?"
choices = [
    "Division of binary numbers",
    "Multiplication of two's complement numbers",
    "Addition of floating-point numbers",
    "Subtraction of binary numbers",
]
answer = "Multiplication of two's complement numbers"
explanations = [
    "",
    "Booth's algorithm efficiently multiplies two's complement binary numbers by examining pairs of bits to reduce the number of addition operations required.",
    "",
    "",
]

[[questions]]
question = "What is the IEEE 754 single-precision (binary32) format size?"
choices = [
    "16 bits",
    "32 bits",
    "64 bits",
    "128 bits",
]
answer = "32 bits"
explanations = [
    "",
    "IEEE 754 single-precision (binary32) uses 32 bits: 1 sign bit, 8 exponent bits, and 23 significand (mantissa) bits.",
    "",
    "",
]

[[questions]]
question = "In IEEE 754 single-precision, what is the bias value for the exponent?"
choices = [
    "64",
    "1023",
    "127",
    "15",
]
answer = "127"
explanations = [
    "",
    "",
    "Single-precision uses a bias of 127. The stored exponent = actual exponent + 127, allowing representation of both positive and negative exponents.",
    "",
]

[[questions]]
question = "What is the purpose of the \"implied bit\" in IEEE 754 normalized numbers?"
choices = [
    "Always 1 and not stored",
    "Always 0",
    "Indicates sign",
    "Indicates exponent",
]
answer = "Always 1 and not stored"
explanations = [
    "In normalized IEEE 754 numbers, the leading 1 of the significand is implied and not stored, effectively giving one extra bit of precision.",
    "",
    "",
    "",
]

[[questions]]
question = "What is a subnormal (denormal) number in IEEE 754?"
choices = [
    "Number with exponent all zeros and non-zero significand",
    "Number with exponent all ones",
    "Normalized number",
    "Infinity",
]
answer = "Number with exponent all zeros and non-zero significand"
explanations = [
    "Subnormal numbers have exponent field all zeros and a non-zero significand, allowing representation of numbers smaller than the smallest normalized number.",
    "",
    "",
    "",
]

[[questions]]
question = "In floating-point addition, what is the first step?"
choices = [
    "Add the significands",
    "Align the binary points by shifting the smaller exponent's significand",
    "Normalize the result",
    "Check for overflow",
]
answer = "Align the binary points by shifting the smaller exponent's significand"
explanations = [
    "",
    "Before adding floating-point numbers, the binary points must be aligned by shifting the significand of the number with the smaller exponent to match the larger exponent.",
    "",
    "",
]

[[questions]]
question = "What is a guard bit in floating-point arithmetic?"
choices = [
    "Bit that protects against overflow",
    "Extra precision bits retained during intermediate calculations",
    "Bit that indicates sign",
    "Bit used for rounding",
]
answer = "Extra precision bits retained during intermediate calculations"
explanations = [
    "",
    "Guard bits are extra bits of precision retained during floating-point operations to improve accuracy before final rounding, reducing rounding errors.",
    "",
    "",
]

[[questions]]
question = "Which rounding mode rounds to the nearest representable number?"
choices = [
    "Round toward +∞",
    "Round toward -∞",
    "Round toward zero",
    "Round to nearest",
]
answer = "Round to nearest"
explanations = [
    "",
    "",
    "",
    "Round to nearest is the default rounding mode in IEEE 754, selecting the closest representable number (ties round to even).",
]

[[questions]]
question = "What is interval arithmetic used for?"
choices = [
    "Improving performance",
    "Monitoring and controlling floating-point errors",
    "Reducing precision",
    "Simplifying calculations",
]
answer = "Monitoring and controlling floating-point errors"
explanations = [
    "",
    "Interval arithmetic produces two values (lower and upper bounds) that contain the true result, with the interval width indicating accuracy and allowing error monitoring.",
    "",
    "",
]

[[questions]]
question = "Who proposed that Boolean algebra could be used for relay-switching circuit design?"
choices = [
    "George Boole",
    "Claude Shannon",
    "John von Neumann",
    "Alan Turing",
]
answer = "Claude Shannon"
explanations = [
    "",
    "While George Boole proposed Boolean algebra in 1854, Claude Shannon suggested its application to relay-switching circuit design in the 1930s.",
    "",
    "",
]

[[questions]]
question = "What is the Boolean expression for the NAND gate?"
choices = [
    "NOT (A AND B)",
    "NOT (A OR B)",
    "(NOT A) AND (NOT B)",
    "(NOT A) OR (NOT B)",
]
answer = "NOT (A AND B)"
explanations = [
    "NAND = NOT AND = (A · B)'. It produces the opposite output of an AND gate: false only when both inputs are true.",
    "",
    "",
    "",
]

[[questions]]
question = "What is the function of a multiplexer?"
choices = [
    "Selects one of several inputs to pass to output based on select lines",
    "Distributes one input to multiple outputs",
    "Performs addition",
    "Stores data",
]
answer = "Selects one of several inputs to pass to output based on select lines"
explanations = [
    "A multiplexer (MUX) selects one of several input signals and routes it to a single output based on control (select) signals. A 4-to-1 MUX has 4 data inputs and 2 select lines.",
    "",
    "",
    "",
]

[[questions]]
question = "What does a decoder do?"
choices = [
    "Converts binary code to a one-hot output",
    "Selects one of several inputs",
    "Performs encoding",
    "Adds binary numbers",
]
answer = "Converts binary code to a one-hot output"
explanations = [
    "A decoder converts an n-bit binary input to a 2ⁿ output where exactly one output is active (1) corresponding to the input value.",
    "",
    "",
    "",
]

[[questions]]
question = "What is the difference between a combinational circuit and a sequential circuit?"
choices = [
    "Combinational circuits have memory; sequential do not",
    "Sequential circuits have memory; combinational outputs depend only on current inputs",
    "Combinational circuits have feedback; sequential do not",
    "Sequential circuits are faster",
]
answer = "Sequential circuits have memory; combinational outputs depend only on current inputs"
explanations = [
    "",
    "Combinational circuits have no memory; output depends only on current inputs. Sequential circuits use memory (flip-flops) and output depends on current inputs and past state.",
    "",
    "",
]

[[questions]]
question = "What is a flip-flop?"
choices = [
    "A combinational circuit",
    "A bistable device that functions as 1-bit memory",
    "A type of logic gate",
    "A multiplexer",
]
answer = "A bistable device that functions as 1-bit memory"
explanations = [
    "",
    "A flip-flop is a bistable sequential circuit that can store one bit. It has two stable states and remains in its current state until a clock edge or control signal changes it.",
    "",
    "",
]

[[questions]]
question = "What is an instruction set?"
choices = [
    "The collection of data types supported by the processor",
    "The collection of different instructions a processor can execute",
    "The set of registers in the CPU",
    "The memory addressing modes",
]
answer = "The collection of different instructions a processor can execute"
explanations = [
    "",
    "The instruction set is the complete collection of instructions that a processor can execute, defining the interface between software and hardware.",
    "",
    "",
]

[[questions]]
question = "What are the four possible locations for operands in machine instructions?"
choices = [
    "Cache, memory, disk, network",
    "Main memory, I/O device, processor register, immediate",
    "Stack, heap, data, code",
    "Local, global, static, dynamic",
]
answer = "Main memory, I/O device, processor register, immediate"
explanations = [
    "",
    "Operands can be in main/virtual memory, I/O devices (with memory-mapped I/O), processor registers, or provided as immediate values in the instruction.",
    "",
    "",
]

[[questions]]
question = "What is a mnemonic in assembly language?"
choices = [
    "A memory address",
    "An abbreviation representing an opcode (e.g., ADD, SUB)",
    "A data value",
    "A comment",
]
answer = "An abbreviation representing an opcode (e.g., ADD, SUB)"
explanations = [
    "",
    "Mnemonics are symbolic abbreviations for machine instructions, such as ADD for addition, SUB for subtraction, making assembly language more readable than binary machine code.",
    "",
    "",
]

[[questions]]
question = "What are the three common numeric data types in computers?"
choices = [
    "Integer, float, double",
    "Binary integer/fixed point, binary floating point, decimal",
    "Byte, word, doubleword",
    "Signed, unsigned, floating",
]
answer = "Binary integer/fixed point, binary floating point, decimal"
explanations = [
    "",
    "Computers commonly support binary integer (fixed-point), binary floating-point, and decimal (packed decimal) numeric data types.",
    "",
    "",
]

[[questions]]
question = "What is packed decimal representation?"
choices = [
    "Each decimal digit stored in one byte",
    "Each decimal digit stored in 4 bits, two digits per byte",
    "Decimal stored as binary",
    "Decimal stored as ASCII",
]
answer = "Each decimal digit stored in 4 bits, two digits per byte"
explanations = [
    "",
    "Packed decimal uses 4-bit BCD (Binary Coded Decimal) codes for each digit, storing two digits per byte for efficient decimal arithmetic.",
    "",
    "",
]

[[questions]]
question = "What is the most commonly used character code?"
choices = [
    "EBCDIC",
    "ASCII (International Reference Alphabet)",
    "Unicode",
    "Baudot code",
]
answer = "ASCII (International Reference Alphabet)"
explanations = [
    "",
    "ASCII (American Standard Code for Information Interchange), which is the US version of the International Reference Alphabet (IRA), is the most common character encoding.",
    "",
    "",
]

[[questions]]
question = "What are SIMD (Single Instruction Multiple Data) data types used for?"
choices = [
    "Single-threaded operations",
    "Optimizing multimedia and vector applications",
    "Character processing",
    "Floating-point only",
]
answer = "Optimizing multimedia and vector applications"
explanations = [
    "",
    "SIMD extensions (MMX, SSE) allow a single instruction to operate on multiple data elements simultaneously, optimizing multimedia and vector applications.",
    "",
    "",
]

[[questions]]
question = "What is the purpose of a skip instruction?"
choices = [
    "To jump to another location unconditionally",
    "To skip the next instruction based on a condition",
    "To call a procedure",
    "To return from a procedure",
]
answer = "To skip the next instruction based on a condition"
explanations = [
    "",
    "Skip instructions test a condition and if true, skip the following instruction, providing a compact way to implement conditional branching.",
    "",
    "",
]

[[questions]]
question = "What are the two basic instructions for procedure handling?"
choices = [
    "JUMP and BRANCH",
    "CALL and RETURN",
    "PUSH and POP",
    "ENTER and LEAVE",
]
answer = "CALL and RETURN"
explanations = [
    "",
    "CALL transfers control to a procedure, saving the return address. RETURN transfers control back to the calling point after procedure completion.",
    "",
    "",
]

[[questions]]
question = "What is a stack frame?"
choices = [
    "A single location in stack",
    "A contiguous block of stack allocated for a procedure's local variables and data",
    "The stack pointer",
    "The base pointer",
]
answer = "A contiguous block of stack allocated for a procedure's local variables and data"
explanations = [
    "",
    "A stack frame (activation record) is a contiguous block of stack memory allocated for a procedure's parameters, return address, local variables, and saved registers.",
    "",
    "",
]

[[questions]]
question = "In immediate addressing, where is the operand located?"
choices = [
    "In a register",
    "In a memory location",
    "In the instruction itself",
    "In the program counter",
]
answer = "In the instruction itself"
explanations = [
    "",
    "",
    "Immediate addressing has the operand value directly in the instruction's address field, eliminating the need for a memory reference to fetch the operand.",
    "",
]

[[questions]]
question = "What is the effective address (EA) in direct addressing?"
choices = [
    "EA = A (the address field value)",
    "EA = (A) (contents of address A)",
    "EA = PC + A",
    "EA = (R) + A",
]
answer = "EA = A (the address field value)"
explanations = [
    "In direct addressing, the effective address is the address field itself (A). The operand is at memory location A.",
    "",
    "",
    "",
]

[[questions]]
question = "What is the advantage of register addressing?"
choices = [
    "Larger address space",
    "No memory reference for operand access",
    "Simpler instruction format",
    "Smaller instruction size",
]
answer = "No memory reference for operand access"
explanations = [
    "",
    "Register addressing accesses operands in registers without a memory reference, making it the fastest addressing mode.",
    "",
    "",
]

[[questions]]
question = "In register indirect addressing, where is the operand located?"
choices = [
    "In the register specified",
    "In a memory location whose address is in the register",
    "In the instruction",
    "In the program counter",
]
answer = "In a memory location whose address is in the register"
explanations = [
    "",
    "Register indirect addressing uses a register that contains the memory address of the operand: EA = (R). This provides large address space with fast register access.",
    "",
    "",
]

[[questions]]
question = "What is the general form of displacement addressing?"
choices = [
    "EA = A",
    "EA = (R)",
    "EA = A + (R)",
    "EA = (A) + (R)",
]
answer = "EA = A + (R)"
explanations = [
    "",
    "",
    "Displacement addressing combines direct addressing (A) and register indirect ((R)): effective address = A + contents of register R.",
    "",
]

[[questions]]
question = "What are the three common uses of displacement addressing?"
choices = [
    "Direct, indirect, immediate",
    "Relative addressing, base-register addressing, indexing",
    "Stack, queue, heap",
    "Preindexing, postindexing, autoindexing",
]
answer = "Relative addressing, base-register addressing, indexing"
explanations = [
    "",
    "The three common displacement addressing types are: relative (PC-relative), base-register (base + displacement), and indexing (base + index).",
    "",
    "",
]

[[questions]]
question = "What is relative addressing?"
choices = [
    "EA = A + PC",
    "EA = (A) + PC",
    "EA = (PC) + A",
    "EA = (A) + (R)",
]
answer = "EA = A + PC"
explanations = [
    "Relative addressing computes effective address by adding the address field to the program counter: EA = A + PC. Useful for position-independent code.",
    "",
    "",
    "",
]

[[questions]]
question = "What is indexing in addressing modes?"
choices = [
    "EA = A + (R) where R is index register",
    "EA = (A) + (R)",
    "EA = A + (R) with auto-increment",
    "EA = (R) + (R')",
]
answer = "EA = A + (R) where R is index register"
explanations = [
    "Indexing uses an index register containing a displacement added to a base address A. It's efficient for array processing and iterative operations.",
    "",
    "",
    "",
]

[[questions]]
question = "What is autoindexing?"
choices = [
    "Automatically increment or decrement the index register after each reference",
    "Indexing without explicit instruction",
    "Index register auto-initialization",
    "Hardware indexing",
]
answer = "Automatically increment or decrement the index register after each reference"
explanations = [
    "Autoindexing automatically increments or decrements the index register after (or before) each use, simplifying loop handling and sequential data access.",
    "",
    "",
    "",
]

[[questions]]
question = "How does stack addressing work?"
choices = [
    "EA is the top of stack",
    "Explicit address field references stack location",
    "Stack pointer implicitly provides effective address",
    "Both A and C",
]
answer = "Both A and C"
explanations = [
    "",
    "",
    "",
    "Stack addressing uses an implicit stack pointer register; operands are implicitly at the top of stack. Push/pop operations manipulate the stack pointer.",
]

[[questions]]
question = "What is the primary characteristic of assembly language?"
choices = [
    "It is hardware independent",
    "One assembly instruction typically translates to one machine instruction",
    "It is the same as machine language",
    "It runs without translation",
]
answer = "One assembly instruction typically translates to one machine instruction"
explanations = [
    "",
    "Assembly language provides a one-to-one mapping between symbolic instructions and machine instructions, making it hardware-dependent and architecture-specific.",
    "",
    "",
]

[[questions]]
question = "What is the main disadvantage of assembly language programming?"
choices = [
    "Too fast execution",
    "Development time, reliability, maintainability, portability issues",
    "Too many instructions",
    "Cannot access hardware directly",
]
answer = "Development time, reliability, maintainability, portability issues"
explanations = [
    "",
    "Assembly language has disadvantages including longer development time, reduced reliability, difficult debugging, poor maintainability, and lack of portability across architectures.",
    "",
    "",
]

[[questions]]
question = "When is assembly language still advantageous?"
choices = [
    "For all applications",
    "For embedded systems, hardware drivers, performance-critical code, and accessing special instructions",
    "Never",
    "Only for simple programs",
]
answer = "For embedded systems, hardware drivers, performance-critical code, and accessing special instructions"
explanations = [
    "",
    "Assembly remains useful for embedded systems, hardware drivers, system code, accessing instructions not available in HLLs, self-modifying code, and optimizing for size/speed.",
    "",
    "",
]

[[questions]]
question = "What is a label in assembly language?"
choices = [
    "A comment",
    "A symbolic name assigned to an address",
    "A mnemonic",
    "An operand",
]
answer = "A symbolic name assigned to an address"
explanations = [
    "",
    "A label is a symbolic name assigned to a memory address, making program locations easier to reference without calculating absolute addresses.",
    "",
    "",
]

[[questions]]
question = "What is a pseudo-instruction (directive)?"
choices = [
    "A machine instruction",
    "An instruction to the assembler, not translated into machine code",
    "A comment",
    "A macro",
]
answer = "An instruction to the assembler, not translated into machine code"
explanations = [
    "",
    "Pseudo-instructions (directives) are commands to the assembler to perform actions during assembly, such as defining constants or reserving memory, not generating machine instructions.",
    "",
    "",
]

[[questions]]
question = "What is the difference between a macro and a subroutine?"
choices = [
    "Macros are faster; subroutines use less memory",
    "Macros expand at assembly time; subroutines are called at runtime",
    "No difference",
    "Macros are hardware; subroutines are software",
]
answer = "Macros expand at assembly time; subroutines are called at runtime"
explanations = [
    "",
    "Macros are expanded by the assembler at assembly time (inline code); subroutines are called at runtime with call/return overhead, saving memory but with execution overhead.",
    "",
    "",
]

[[questions]]
question = "What are the five basic processor requirements?"
choices = [
    "Input, output, storage, processing, control",
    "Fetch instruction, interpret instruction, fetch data, process data, write data",
    "Read, write, execute, store, transfer",
    "Load, store, add, subtract, branch",
]
answer = "Fetch instruction, interpret instruction, fetch data, process data, write data"
explanations = [
    "",
    "The processor must fetch instructions, interpret them, fetch required data, process the data, and write results back to memory or I/O.",
    "",
    "",
]

[[questions]]
question = "What is the function of the Program Counter (PC)?"
choices = [
    "Stores the current instruction",
    "Contains the address of the next instruction to fetch",
    "Stores data from memory",
    "Holds the memory address for I/O",
]
answer = "Contains the address of the next instruction to fetch"
explanations = [
    "",
    "The Program Counter holds the address of the next instruction to be fetched from memory, automatically incrementing after each fetch.",
    "",
    "",
]

[[questions]]
question = "What is the Instruction Register (IR)?"
choices = [
    "Contains the address of the next instruction",
    "Contains the instruction most recently fetched",
    "Contains the result of arithmetic operations",
    "Contains status flags",
]
answer = "Contains the instruction most recently fetched"
explanations = [
    "",
    "The Instruction Register holds the instruction currently being executed, allowing the control unit to decode and execute it.",
    "",
    "",
]

[[questions]]
question = "What is the Program Status Word (PSW)?"
choices = [
    "Contains the program counter",
    "Contains status information including condition codes and control flags",
    "Contains the instruction being executed",
    "Contains memory protection information",
]
answer = "Contains status information including condition codes and control flags"
explanations = [
    "",
    "The PSW contains condition codes (flags), interrupt enable/disable bits, privilege level, and other status information controlling processor operation.",
    "",
    "",
]

[[questions]]
question = "What are the two categories of processor registers?"
choices = [
    "Data and address registers",
    "User-visible registers and control/status registers",
    "General-purpose and special-purpose",
    "Integer and floating-point",
]
answer = "User-visible registers and control/status registers"
explanations = [
    "",
    "Processor registers are classified as user-visible registers (accessible to programmers) and control/status registers (used by control unit and OS).",
    "",
    "",
]

[[questions]]
question = "What are condition codes?"
choices = [
    "Codes for conditional branching",
    "Bits set by ALU operations indicating results (zero, carry, overflow, sign)",
    "Instruction opcodes",
    "Memory protection bits",
]
answer = "Bits set by ALU operations indicating results (zero, carry, overflow, sign)"
explanations = [
    "",
    "Condition codes (status flags) are bits in the PSW set by arithmetic/logical operations to indicate results: zero, carry, overflow, sign, etc., used for conditional execution.",
    "",
    "",
]

[[questions]]
question = "What is pipelining in processor design?"
choices = [
    "Connecting processors in a line",
    "Overlapping instruction execution stages to improve throughput",
    "A type of memory",
    "A bus architecture",
]
answer = "Overlapping instruction execution stages to improve throughput"
explanations = [
    "",
    "Pipelining overlaps the execution of multiple instructions by dividing the instruction cycle into stages, allowing different instructions to be at different stages simultaneously.",
    "",
    "",
]

[[questions]]
question = "In a pipeline, what is a structural hazard?"
choices = [
    "When data dependencies cause stalls",
    "When instructions need the same hardware resource simultaneously",
    "When branch prediction is wrong",
    "When cache is missed",
]
answer = "When instructions need the same hardware resource simultaneously"
explanations = [
    "",
    "Structural hazards occur when multiple instructions in the pipeline require the same hardware resource (e.g., same memory port) at the same time.",
    "",
    "",
]

[[questions]]
question = "What is a data hazard?"
choices = [
    "Instructions accessing same data causing dependency",
    "Hardware resource conflict",
    "Branch misprediction",
    "Cache miss",
]
answer = "Instructions accessing same data causing dependency"
explanations = [
    "Data hazards occur when instructions depend on results of previous instructions that have not yet completed, potentially causing incorrect execution if not handled.",
    "",
    "",
    "",
]

[[questions]]
question = "What is a Read After Write (RAW) hazard?"
choices = [
    "Instruction reads before previous write completes",
    "Instruction writes after previous read",
    "Two writes to same location",
    "Branch causing flush",
]
answer = "Instruction reads before previous write completes"
explanations = [
    "RAW (true dependency) occurs when a later instruction reads a value that an earlier instruction is still computing, requiring the read to wait for the write.",
    "",
    "",
    "",
]

[[questions]]
question = "What is a control hazard (branch hazard)?"
choices = [
    "Data dependency",
    "Pipeline making wrong branch prediction causing instruction discard",
    "Resource conflict",
    "Cache miss",
]
answer = "Pipeline making wrong branch prediction causing instruction discard"
explanations = [
    "",
    "Control hazards occur when the pipeline makes the wrong prediction on a conditional branch, fetching instructions that must be discarded when branch outcome is known.",
    "",
    "",
]

[[questions]]
question = "What is branch prediction?"
choices = [
    "Predicting whether a branch will be taken to keep pipeline full",
    "Calculating branch target",
    "Delaying branch execution",
    "Eliminating branches",
]
answer = "Predicting whether a branch will be taken to keep pipeline full"
explanations = [
    "Branch prediction guesses whether a conditional branch will be taken, allowing the pipeline to fetch instructions from the predicted path and avoid stalls.",
    "",
    "",
    "",
]

[[questions]]
question = "What are the three primary characteristics of RISC architectures?"
choices = [
    "Complex instructions, microcode, variable-length instructions",
    "Large register file, optimized pipeline, predictable instruction costs",
    "Memory-memory operations, complex addressing, microprogrammed control",
    "Variable-length instructions, complex ALU, microcode",
]
answer = "Large register file, optimized pipeline, predictable instruction costs"
explanations = [
    "",
    "RISC architectures feature: 1) large register file or compiler register optimization, 2) careful pipeline design, and 3) instructions with predictable costs for high-performance implementation.",
    "",
    "",
]

[[questions]]
question = "What is the primary motivation for using a large register file in RISC?"
choices = [
    "To reduce cost",
    "To minimize memory references by keeping more variables in registers",
    "To increase instruction count",
    "To simplify compiler design",
]
answer = "To minimize memory references by keeping more variables in registers"
explanations = [
    "",
    "A large register file allows more variables to be kept in registers, reducing memory accesses which are much slower than register accesses.",
    "",
    "",
]

[[questions]]
question = "What are overlapping register windows?"
choices = [
    "Register windows that share memory",
    "Overlapping sets of registers for procedure calls to reduce saving/restoring",
    "Windows that display registers",
    "Overlapping address spaces",
]
answer = "Overlapping sets of registers for procedure calls to reduce saving/restoring"
explanations = [
    "",
    "Overlapping register windows allow parameters to be passed between procedures without saving/restoring by having overlapping regions between caller and callee register sets.",
    "",
    "",
]

[[questions]]
question = "What is the trade-off between large register file and cache?"
choices = [
    "Cache is always better",
    "Register file provides lower latency; cache handles larger working sets",
    "No difference",
    "Register file is larger",
]
answer = "Register file provides lower latency; cache handles larger working sets"
explanations = [
    "",
    "Registers provide faster access than cache but are limited in number. Cache can handle larger working sets but with higher latency. A balanced design may use both.",
    "",
    "",
]

[[questions]]
question = "Why did CISC (Complex Instruction Set Computer) architectures develop?"
choices = [
    "To simplify hardware",
    "To simplify compilers and improve performance with smaller programs",
    "To increase instruction count",
    "To reduce register usage",
]
answer = "To simplify compilers and improve performance with smaller programs"
explanations = [
    "",
    "CISC architectures evolved to simplify compilers by providing complex instructions close to HLL statements and to improve performance with smaller programs requiring fewer instruction fetches.",
    "",
    "",
]

[[questions]]
question = "What is delayed load in RISC pipelines?"
choices = [
    "Load instruction is delayed",
    "Register to be loaded is locked; execution continues until register needed",
    "Load is postponed",
    "Load uses delayed addressing",
]
answer = "Register to be loaded is locked; execution continues until register needed"
explanations = [
    "",
    "Delayed load allows the processor to continue executing instructions after a load while the target register is locked, useful work can be done during the load latency.",
    "",
    "",
]

[[questions]]
question = "What is loop unrolling?"
choices = [
    "Replicating loop body multiple times to reduce loop overhead",
    "Rolling loop into single iteration",
    "Removing loops",
    "Adding loops",
]
answer = "Replicating loop body multiple times to reduce loop overhead"
explanations = [
    "Loop unrolling replicates the loop body multiple times, reducing the number of loop iterations and loop overhead, improving instruction parallelism and locality.",
    "",
    "",
    "",
]

[[questions]]
question = "What is a superpipelined architecture?"
choices = [
    "Multiple pipelines in parallel",
    "More fine-grained pipeline stages with higher clock frequency",
    "Wider pipelines",
    "Longer pipelines",
]
answer = "More fine-grained pipeline stages with higher clock frequency"
explanations = [
    "",
    "Superpipelining uses more, finer-grained pipeline stages, allowing higher clock frequencies and more instructions in the pipeline simultaneously.",
    "",
    "",
]

[[questions]]
question = "What is a superscalar architecture?"
choices = [
    "Single instruction per cycle",
    "Multiple pipelines allowing multiple instructions per cycle",
    "Very long instruction words",
    "Deep pipelines",
]
answer = "Multiple pipelines allowing multiple instructions per cycle"
explanations = [
    "",
    "Superscalar architectures replicate pipeline stages so multiple instructions can be processed simultaneously, achieving more than one instruction per cycle.",
    "",
    "",
]

[[questions]]
question = "What is a reservation station in pipeline organization?"
choices = [
    "Station for waiting instructions",
    "Buffer between instruction decode and functional units for out-of-order execution",
    "Memory for reservations",
    "Branch prediction buffer",
]
answer = "Buffer between instruction decode and functional units for out-of-order execution"
explanations = [
    "",
    "Reservation stations buffer instructions between decode and execution units, allowing instructions to wait for their operands and be dispatched when ready.",
    "",
    "",
]

[[questions]]
question = "What is data forwarding (bypassing)?"
choices = [
    "Sending data to next instruction without waiting for write back",
    "Forwarding data to memory",
    "Branch forwarding",
    "Cache forwarding",
]
answer = "Sending data to next instruction without waiting for write back"
explanations = [
    "Data forwarding makes results available to subsequent instructions as soon as they are produced, bypassing the write-back stage to eliminate RAW hazards.",
    "",
    "",
    "",
]

[[questions]]
question = "What is a reorder buffer (ROB)?"
choices = [
    "Buffer for reordering instructions",
    "Buffer that supports out-of-order execution by allowing instructions to complete in order",
    "Buffer for branch targets",
    "Cache buffer",
]
answer = "Buffer that supports out-of-order execution by allowing instructions to complete in order"
explanations = [
    "",
    "The reorder buffer allows instructions to execute out-of-order but commit results in program order, supporting precise exceptions and correct program state.",
    "",
    "",
]

[[questions]]
question = "What is out-of-order execution (OoOE)?"
choices = [
    "Instructions executed in any order",
    "Instructions begin execution as soon as operands ready, not necessarily program order",
    "Random execution order",
    "Reverse order execution",
]
answer = "Instructions begin execution as soon as operands ready, not necessarily program order"
explanations = [
    "",
    "Out-of-order execution allows instructions to start execution when their operands are ready, potentially reordering them to avoid stalls while maintaining correct final results.",
    "",
    "",
]

[[questions]]
question = "What is instruction-level parallelism (ILP)?"
choices = [
    "Parallel execution of instructions from different programs",
    "The degree to which instructions in a program can be executed in parallel",
    "Parallel execution on multiple processors",
    "Parallel execution of threads",
]
answer = "The degree to which instructions in a program can be executed in parallel"
explanations = [
    "",
    "ILP refers to the potential for overlapping the execution of instructions within a program stream, determined by dependencies and available hardware resources.",
    "",
    "",
]

[[questions]]
question = "What is the difference between ILP and machine parallelism?"
choices = [
    "ILP is software; machine parallelism is hardware",
    "ILP is the parallelism in the program; machine parallelism is the hardware's ability to exploit it",
    "No difference",
    "ILP is hardware; machine parallelism is software",
]
answer = "ILP is the parallelism in the program; machine parallelism is the hardware's ability to exploit it"
explanations = [
    "",
    "ILP is inherent in the instruction sequence; machine parallelism is the hardware's capacity to execute multiple instructions simultaneously, limited by pipeline resources.",
    "",
    "",
]

[[questions]]
question = "What is an antidependency (Write After Read - WAR)?"
choices = [
    "Instruction reads before previous write",
    "Instruction writes after previous read",
    "Two writes to same location",
    "Branch dependency",
]
answer = "Instruction writes after previous read"
explanations = [
    "",
    "WAR occurs when an instruction writes to a location that a previous instruction has read; reversing order would cause incorrect execution.",
    "",
    "",
]

[[questions]]
question = "What is register renaming?"
choices = [
    "Changing register names",
    "Mapping architectural registers to a larger set of physical registers to eliminate false dependencies",
    "Renaming instructions",
    "Register allocation",
]
answer = "Mapping architectural registers to a larger set of physical registers to eliminate false dependencies"
explanations = [
    "",
    "Register renaming eliminates WAR and WAW hazards by mapping architectural registers to a larger pool of physical registers, allowing multiple uses of the same logical register without conflict.",
    "",
    "",
]

[[questions]]
question = "What are the three types of instruction issue policies?"
choices = [
    "Single, multiple, parallel",
    "In-order issue with in-order completion, in-order with out-of-order completion, out-of-order with out-of-order completion",
    "Scalar, superscalar, superpipeline",
    "Static, dynamic, hybrid",
]
answer = "In-order issue with in-order completion, in-order with out-of-order completion, out-of-order with out-of-order completion"
explanations = [
    "",
    "The three instruction issue policies differ in whether instructions are issued and completed in program order or can be reordered for performance.",
    "",
    "",
]

[[questions]]
question = "In the Intel Core microarchitecture, what are micro-ops?"
choices = [
    "Very small operations",
    "RISC-like operations that x86 instructions are decoded into (1-4 per instruction)",
    "Microcode instructions",
    "Pipeline stages",
]
answer = "RISC-like operations that x86 instructions are decoded into (1-4 per instruction)"
explanations = [
    "",
    "Complex x86 instructions are decoded into simpler RISC-like micro-ops (118-bit each) that can be executed by the out-of-order execution engine.",
    "",
    "",
]

[[questions]]
question = "What limits ILP in programs?"
choices = [
    "Only hardware resources",
    "True data dependencies, procedural dependencies, resource conflicts",
    "Only branch instructions",
    "Only memory accesses",
]
answer = "True data dependencies, procedural dependencies, resource conflicts"
explanations = [
    "",
    "ILP is limited by: true data dependencies (RAW), procedural dependencies (branches), resource conflicts (structural hazards), output dependencies (WAW), and antidependencies (WAR).",
    "",
    "",
]

[[questions]]
question = "What are micro-operations?"
choices = [
    "Very small instructions",
    "The functional, atomic operations of a processor",
    "Microcode instructions",
    "Pipeline stages",
]
answer = "The functional, atomic operations of a processor"
explanations = [
    "",
    "Micro-operations are the fundamental, atomic operations (e.g., register transfers, ALU operations) that a processor performs during instruction execution.",
    "",
    "",
]

[[questions]]
question = "What is the sequence of micro-operations for the fetch cycle?"
choices = [
    "MAR ← PC, MBR ← Memory, IR ← MBR, PC ← PC + 1",
    "IR ← MAR, PC ← PC + 1",
    "MAR ← IR, MBR ← Memory",
    "PC ← MAR, IR ← MBR",
]
answer = "MAR ← PC, MBR ← Memory, IR ← MBR, PC ← PC + 1"
explanations = [
    "Fetch cycle: MAR ← PC (address to memory), MBR ← Memory (read instruction), IR ← MBR (load instruction), PC ← PC + 1 (increment for next instruction).",
    "",
    "",
    "",
]

[[questions]]
question = "What are the two basic tasks of the control unit?"
choices = [
    "Fetch and execute",
    "Sequencing and execution",
    "Decode and execute",
    "Read and write",
]
answer = "Sequencing and execution"
explanations = [
    "",
    "The control unit performs sequencing (determining next micro-operation) and execution (generating control signals to perform micro-operations).",
    "",
    "",
]

[[questions]]
question = "What is the difference between hardwired and microprogrammed control?"
choices = [
    "Hardwired is software; microprogrammed is hardware",
    "Hardwired uses logic circuits; microprogrammed uses microcode in control memory",
    "No difference",
    "Hardwired is faster; microprogrammed is slower",
]
answer = "Hardwired uses logic circuits; microprogrammed uses microcode in control memory"
explanations = [
    "",
    "Hardwired control uses fixed logic circuits (state machine); microprogrammed control uses microcode stored in control memory that generates control signals.",
    "",
    "",
]

[[questions]]
question = "What is a microinstruction?"
choices = [
    "A machine instruction",
    "A single micro-operation encoding",
    "A word in control memory that specifies one or more micro-operations",
    "A microcode instruction",
]
answer = "A word in control memory that specifies one or more micro-operations"
explanations = [
    "",
    "",
    "A microinstruction is a word stored in control memory that specifies control signals to execute one or more micro-operations.",
    "",
]

[[questions]]
question = "What is the advantage of microprogramming?"
choices = [
    "Faster execution",
    "Simpler hardware, easier to design and modify",
    "Less control memory",
    "Smaller processor",
]
answer = "Simpler hardware, easier to design and modify"
explanations = [
    "",
    "Microprogramming simplifies control unit design, makes it systematic, easier to modify, and allows complex instructions to be implemented with structured microcode.",
    "",
    "",
]

[[questions]]
question = "What does SIMD (Single Instruction, Multiple Data) represent?"
choices = [
    "Multiple processors executing different instructions",
    "Single instruction controlling multiple processing elements on different data",
    "Vector processor only",
    "Multiprocessor",
]
answer = "Single instruction controlling multiple processing elements on different data"
explanations = [
    "",
    "SIMD executes the same instruction across multiple processing elements simultaneously on different data, typical of vector and array processors.",
    "",
    "",
]

[[questions]]
question = "What does MIMD (Multiple Instruction, Multiple Data) represent?"
choices = [
    "Single processor",
    "Multiple processors executing different instruction streams on different data",
    "Vector processor",
    "SIMD machine",
]
answer = "Multiple processors executing different instruction streams on different data"
explanations = [
    "",
    "MIMD represents multiprocessor systems where multiple processors independently execute different instructions on different data, including SMPs and clusters.",
    "",
    "",
]

[[questions]]
question = "What is a Symmetric Multiprocessor (SMP)?"
choices = [
    "Multiple processors sharing a single memory with symmetric access",
    "Processors with private memories",
    "Processors arranged in a ring",
    "Asymmetric processing",
]
answer = "Multiple processors sharing a single memory with symmetric access"
explanations = [
    "SMP systems have multiple processors sharing a single main memory with symmetric access, typically connected via a shared bus or crossbar.",
    "",
    "",
    "",
]

[[questions]]
question = "What is the main disadvantage of bus-based SMP?"
choices = [
    "Complexity",
    "Performance limited by bus bandwidth",
    "High cost",
    "Unreliable",
]
answer = "Performance limited by bus bandwidth"
explanations = [
    "",
    "All memory references go through the common bus, making bus bandwidth the primary performance limitation, especially with many processors.",
    "",
    "",
]

[[questions]]
question = "What is cache coherence?"
choices = [
    "Caches having same data",
    "Ensuring multiple caches have consistent copies of shared data",
    "Cache synchronization",
    "Cache sharing",
]
answer = "Ensuring multiple caches have consistent copies of shared data"
explanations = [
    "",
    "Cache coherence ensures that when one processor updates data in its cache, other caches containing the same data are updated or invalidated to maintain consistency.",
    "",
    "",
]

[[questions]]
question = "What are the two categories of hardware cache coherence protocols?"
choices = [
    "MESI and MOESI",
    "Directory protocols and snoopy protocols",
    "Write-through and write-back",
    "Inclusive and exclusive",
]
answer = "Directory protocols and snoopy protocols"
explanations = [
    "",
    "Hardware cache coherence solutions are divided into directory protocols (centralized directory tracking) and snoopy protocols (broadcast-based).",
    "",
    "",
]

[[questions]]
question = "What is the MESI protocol?"
choices = [
    "Memory protocol",
    "Cache coherence protocol with states: Modified, Exclusive, Shared, Invalid",
    "Multiprocessor protocol",
    "Bus arbitration protocol",
]
answer = "Cache coherence protocol with states: Modified, Exclusive, Shared, Invalid"
explanations = [
    "",
    "MESI is a snoopy cache coherence protocol using four states per cache line: Modified, Exclusive, Shared, Invalid to track cache line status.",
    "",
    "",
]

[[questions]]
question = "In MESI protocol, what does the \"Modified\" state indicate?"
choices = [
    "Line is same as memory, may be in other caches",
    "Line has been modified and is only in this cache (dirty)",
    "Line is invalid",
    "Line is exclusive to this cache",
]
answer = "Line has been modified and is only in this cache (dirty)"
explanations = [
    "",
    "Modified means the cache line has been modified and is present only in this cache; main memory copy is stale (dirty).",
    "",
    "",
]

[[questions]]
question = "In MESI protocol, what does the \"Exclusive\" state indicate?"
choices = [
    "Line is modified",
    "Line matches main memory and is only in this cache",
    "Line may be in other caches",
    "Line is invalid",
]
answer = "Line matches main memory and is only in this cache"
explanations = [
    "",
    "Exclusive means the cache line matches main memory and is present only in this cache; no other caches have a copy (clean, exclusive).",
    "",
    "",
]

[[questions]]
question = "In MESI protocol, what does the \"Shared\" state indicate?"
choices = [
    "Line is modified",
    "Line matches main memory and may be in other caches",
    "Line is exclusive",
    "Line is invalid",
]
answer = "Line matches main memory and may be in other caches"
explanations = [
    "",
    "Shared means the cache line matches main memory and may be present in multiple caches; all copies are consistent with main memory.",
    "",
    "",
]

[[questions]]
question = "What is multithreading?"
choices = [
    "Multiple threads in one process",
    "Dividing instruction stream into multiple streams executed in parallel",
    "Multiple processes",
    "Multiple cores",
]
answer = "Dividing instruction stream into multiple streams executed in parallel"
explanations = [
    "",
    "Multithreading divides a program into multiple threads that can be executed in parallel, increasing instruction-level parallelism without increasing circuit complexity.",
    "",
    "",
]

[[questions]]
question = "What is Simultaneous Multithreading (SMT)?"
choices = [
    "Threads executed sequentially",
    "Instructions from multiple threads issued simultaneously to superscalar execution units",
    "One thread at a time",
    "Time-sliced threads",
]
answer = "Instructions from multiple threads issued simultaneously to superscalar execution units"
explanations = [
    "",
    "SMT (e.g., Intel Hyper-Threading) issues instructions from multiple threads to the execution units of a superscalar processor in the same cycle.",
    "",
    "",
]

[[questions]]
question = "What is a cluster?"
choices = [
    "Single computer",
    "Group of interconnected whole computers working as unified resource",
    "Multiple cores",
    "NUMA system",
]
answer = "Group of interconnected whole computers working as unified resource"
explanations = [
    "",
    "A cluster is a group of interconnected complete computers (nodes) that work together as a unified computing resource, providing scalability and high availability.",
    "",
    "",
]

[[questions]]
question = "What is Direct Data Intervention (DDI) in ARM Cortex-A15?"
choices = [
    "Direct memory access",
    "Enables copying clean data between L1 caches without accessing external memory",
    "Data transfer between processors",
    "Cache bypass",
]
answer = "Enables copying clean data between L1 caches without accessing external memory"
explanations = [
    "",
    "DDI allows L1 caches to transfer data directly to other L1 caches, reducing latency and memory bandwidth usage for coherence operations.",
    "",
    "",
]

[[questions]]
question = "What are duplicated tag RAMs used for in ARM Cortex-A15?"
choices = [
    "Redundant storage",
    "Cache tags duplicated for Snoop Control Unit to check data availability without accessing full cache",
    "Increased cache capacity",
    "Error correction",
]
answer = "Cache tags duplicated for Snoop Control Unit to check data availability without accessing full cache"
explanations = [
    "",
    "Duplicated tag RAMs allow the SCU to check data availability before sending coherency commands, reducing unnecessary coherence traffic.",
    "",
    "",
]

[[questions]]
question = "What is the Snoop Control Unit (SCU) in ARM Cortex-A15?"
choices = [
    "Control unit for snooping",
    "Hardware that manages L1 cache coherency and reduces scalability bottlenecks",
    "Bus controller",
    "Memory controller",
]
answer = "Hardware that manages L1 cache coherency and reduces scalability bottlenecks"
explanations = [
    "",
    "The SCU manages cache coherence between L1 caches, implements the MESI protocol, and provides DDI and duplicated tags to improve scalability.",
    "",
    "",
]

[[questions]]
question = "What is a \"migratory line\" in cache coherence?"
choices = [
    "Moving data between cores",
    "Allows moving dirty data between CPUs without writing to L2 and reading back",
    "Cache line migration",
    "Data migration",
]
answer = "Allows moving dirty data between CPUs without writing to L2 and reading back"
explanations = [
    "",
    "Migratory lines enable efficient transfer of dirty data between processors, avoiding the overhead of writing to L2 and reading back from external memory.",
    "",
    "",
]

[[questions]]
question = "What is false sharing?"
choices = [
    "Sharing that doesn't exist",
    "When unrelated data in same cache line cause unnecessary coherence traffic",
    "False memory sharing",
    "Cache line sharing",
]
answer = "When unrelated data in same cache line cause unnecessary coherence traffic"
explanations = [
    "",
    "False sharing occurs when multiple processors access different variables that happen to reside in the same cache line, causing unnecessary coherence traffic and performance degradation.",
    "",
    "",
]

[[questions]]
question = "(Chapter 11) In floating-point representation, what is a normalized number?"
choices = [
    "Significand has leading 1 (in binary)",
    "Exponent is zero",
    "Number is zero",
    "Significand is zero",
]
answer = "Significand has leading 1 (in binary)"
explanations = [
    "Normalized floating-point numbers have the most significant digit of the significand non-zero; in binary, this means the leading bit is 1 (implied in IEEE 754).",
    "",
    "",
    "",
]


